#!/bin/bash 

if [ -z "$PGDATA" ]; then
    echo 'Warning: $PGDATA is not set!'
    exit 1
fi

# Development
nohup ~/local/bin/postgres &> ~/log/start-postgresql.log &
# Production
#nohup ~/local/bin/postgres -r ~/log/postgres.log > /dev/null  &
